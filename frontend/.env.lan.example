# ======================================================================
# EQUITY ARENA FRONTEND — LOCAL LAB LAN MODE ENVIRONMENT CONFIGURATION
# Copy this file to `frontend/.env` for local college lab WiFi events
# ======================================================================

# Base URL pointing to the laptop backend running on local LAN
# Replace 192.168.x.x with your laptop's actual LAN IP on the day of the event
VITE_API_URL="http://192.168.x.x:5001"
